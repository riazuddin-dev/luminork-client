# Production build env (committed — public API URL only, no secrets)
# Used by `next build` / Vercel production deploys.
# Client normalizes host → .../api if /api is omitted.

NEXT_PUBLIC_API_URL=https://luminork-server.vercel.app
NEXT_PUBLIC_SITE_URL=https://luminork-client.vercel.app
